/**
 * config.example.js — plantilla para desarrollo LOCAL.
 *
 * En Vercel, config.js se genera solo (ver build-config.js) a partir de
 * las variables de entorno del proyecto: no hace falta tocar nada acá
 * para producción.
 *
 * Para probar la app en tu computadora:
 *   1. Copiá este archivo y renombralo a `config.js` (en la misma carpeta).
 *   2. Completá los dos valores de abajo con los de tu proyecto de Supabase
 *      (Project Settings → API en el panel de Supabase).
 *   3. NO subas tu config.js real a un repositorio público si preferís no
 *      exponer esos valores ahí (aunque la clave publicable está diseñada
 *      para poder usarse en el navegador sin riesgo, a diferencia de la
 *      "service_role key", que jamás debe usarse en este proyecto).
 */
window.FXL_SUPABASE_URL = 'https://wpugmybtretxcdiowasj.supabase.co';
window.FXL_SUPABASE_ANON_KEY = 'PEGÁ_ACÁ_TU_PUBLISHABLE_KEY';
